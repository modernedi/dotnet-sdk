<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup Condition="'$(UsePackedSdk)' != 'true'">
    <ProjectReference Include="../../src/ModernEdi/ModernEdi.csproj" />
  </ItemGroup>
  <ItemGroup Condition="'$(UsePackedSdk)' == 'true'">
    <PackageReference Include="ModernEdi" Version="0.2.1" />
  </ItemGroup>
</Project>
